apiVersion: v1
kind: Service
metadata:
  name: {{ include "highway.fullname" . }}
  labels:
    {{- include "highway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "highway.selectorLabels" . | nindent 4 }}
