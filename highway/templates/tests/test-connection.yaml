apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "highway.fullname" . }}-test-connection"
  labels:
    {{- include "highway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "highway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
